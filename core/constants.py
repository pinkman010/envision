"""全局常量定义

此模块包含项目中使用的所有常量，避免魔法数字和硬编码字符串。
"""

# ============================================================================
# 评分与阈值常量
# ============================================================================

# 默认维度得分（当数据缺失时）
DEFAULT_DIMENSION_SCORE = 50.0

# 置信度等级阈值
CONFIDENCE_THRESHOLDS = {
    "极低": 0.0,
    "低": 0.3,
    "中": 0.6,
    "较高": 0.8,
    "高": 1.0
}

# 差距分析阈值
GAP_THRESHOLD_HIGH = 15.0
GAP_THRESHOLD_MEDIUM = 8.0

# AHP一致性检验阈值
AHP_CONSISTENCY_THRESHOLD = 0.1

# 数据质量校验阈值
DATA_QUALITY_THRESHOLDS = {
    "min_carbon_emissions": 1000,
    "min_employees": 100,
    "min_carbon_per_employee": 50000
}

# ============================================================================
# ESG维度定义
# ============================================================================

ESG_DIMENSIONS = {
    "E": {"name": "环境", "color": "#52c41a"},
    "S": {"name": "社会", "color": "#1890ff"},
    "G": {"name": "治理", "color": "#faad14"}
}

# 维度名称简写映射
ESG_DIMENSION_NAMES = {
    "E": "环境",
    "S": "社会",
    "G": "治理"
}

# ============================================================================
# AHP相关常量
# ============================================================================

# 随机一致性指标RI表（Saaty标准）
AHP_RI_TABLE = {
    1: 0.00,
    2: 0.00,
    3: 0.58,
    4: 0.90,
    5: 1.12,
    6: 1.24,
    7: 1.32,
    8: 1.41,
    9: 1.45,
    10: 1.49
}

# AHP重要性标度描述
AHP_SCALE_LABELS = {
    1: "同等重要",
    3: "稍微重要",
    5: "明显重要",
    7: "强烈重要",
    9: "极端重要"
}

# ============================================================================
# UI相关常量
# ============================================================================

# 分析年度选项
ANALYSIS_YEARS = ["2025", "2024", "2023"]

# 对标企业选项
BENCHMARK_COMPANIES = ["维斯塔斯", "西门子歌美飒", "行业平均"]

# 评估视角选项
EVALUATION_PERSPECTIVES = {
    "financial": {"name": "财务稳健性（投资者视角）", "weights": {"E": 0.25, "S": 0.30, "G": 0.45}},
    "compliance": {"name": "合规与风险（监管视角）", "weights": {"E": 0.40, "S": 0.25, "G": 0.35}},
    "brand": {"name": "品牌影响力（公众视角）", "weights": {"E": 0.30, "S": 0.45, "G": 0.25}},
    "balanced": {"name": "均衡配置", "weights": {"E": 0.333, "S": 0.333, "G": 0.334}}
}

# ============================================================================
# 文件路径常量
# ============================================================================

import os

PROJECT_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
DATA_DIR = os.path.join(PROJECT_ROOT, "data")
MOCK_DATA_DIR = os.path.join(DATA_DIR, "mock")

# Mock数据文件路径
MOCK_TOPICS_FILE = os.path.join(MOCK_DATA_DIR, "esg_topics.json")
MOCK_BENCHMARK_FILE = os.path.join(MOCK_DATA_DIR, "benchmark_data.json")
