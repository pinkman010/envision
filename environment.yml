name: esg-analysis
channels:
  - conda-forge

dependencies:
  # Python 解释器版本（严格锁定）
  - python=3.11
  
  # 系统级二进制库
  # pdfplumber 需要 poppler 进行 PDF 解析
  - poppler>=23.0
  
  # pip 本身（必须，用于委托）
  - pip
  
  # 通过 pip 安装当前项目（关键：委托给 pyproject.toml）
  - pip:
    - -e ".[dev]"
