name: envision
channels:
  - conda-forge

dependencies:
  # Python 解释器版本
  - python=3.13
  
  # 系统级二进制库
  # pdfplumber 需要 poppler 进行 PDF 解析
  - poppler>=23.0
  
  # pip 本身
  - pip
  
  # 通过 pip 安装当前项目（关键：委托给 pyproject.toml）
  - pip:
    - -e ".[dev]"
