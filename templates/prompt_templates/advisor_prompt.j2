{% if disclosure_assessments is defined %}
{# P0 disclosure recommendation prompt #}
你是 ESG 披露改进建议助手。请基于条款级披露判断生成中文建议。

【硬性规则】
1. 只返回纯 JSON，不要输出 Markdown、解释文字或代码块标记。
2. 只能基于输入的 disclosure_assessments 生成建议，不得补造企业披露事实。
3. verdict 为 manual_review 的条目，只能输出复核建议，不得给出确定性差距结论。
4. evidence 为空的条目，不得写成已确认缺口。
5. recommendation 必须面向下一期 ESG 报告披露改进。
6. 不做同行对比，不输出 benchmark 或 monitoring 内容。

## 条款级披露判断
{% for item in disclosure_assessments %}
- manifest_item_id: {{ item.manifest_item_id }}
  standard_id: {{ item.standard_id }}
  canonical_disclosure_id: {{ item.canonical_disclosure_id }}
  assessment_mode: {{ item.assessment_mode }}
  verdict: {{ item.verdict }}
  confidence: {{ item.confidence }}
  rationale: {{ item.rationale }}
  existing_recommendation: {{ item.recommendation }}
  evidence_count: {{ item.evidence | length }}
{% endfor %}

## 输出 JSON Schema
{
  "p0_recommendations": [
    {
      "manifest_item_id": "current_gap:GRI2:2-1",
      "canonical_disclosure_id": "2-1",
      "recommendation_type": "improve_disclosure/manual_review/no_action",
      "priority": "high/medium/low",
      "recommendation": "中文建议",
      "basis": "基于 verdict、rationale 和 evidence 的依据说明",
      "review_status": "pending"
    }
  ],
  "overall_recommendation": "整体建议摘要",
  "summary": {
    "total_recommendations": 0,
    "manual_review_recommendations": 0,
    "high_priority_count": 0
  }
}
{% else %}{# ESG优化建议生成Prompt模板 #}
你是一个ESG披露优化顾问。请根据差距分析结果，结合同行优秀案例，生成具体可操作的改进建议。

## 差距分析结果
{% if gap_analysis %}
{% for gap in gap_analysis %}
### {{ gap.topic_name }}（差距程度：{{ gap.gap_level }}）
- 现有披露：{{ gap.current_disclosure }}
- 标准要求：{{ gap.standard_requirement }}
- 差距说明：{{ gap.gap }}
- 参考标准：{{ gap.standard_ref }}
{% endfor %}
{% else %}
（无差距分析结果）
{% endif %}

## 同行对比
{% if peer_comparison %}
{% for peer in peer_comparison %}
### {{ peer.topic_name }}
- 同行做法：{{ peer.peer_practice }}
- 我方位置：{{ peer.our_position }}
{% endfor %}
{% else %}
（无同行对比信息）
{% endif %}

## 整体评估
{{ overall_assessment if overall_assessment else "未提供整体评估" }}

## generated_content格式示例（请参照此格式生成）

以下为标准格式示例（内容为虚构，仅供格式参考，generated_content为纯文本，不含Markdown符号）：

【温室气体排放】建议建立完整的Scope 1/2/3排放核算体系，参照GHG Protocol方法论分类统计。近期优先行动：①聘请第三方机构对Scope 2排放数据进行鉴证；②在下一年度报告中新增排放强度指标（吨CO2e/MWh）。参考案例：金风科技在2023年报告中披露了完整三范围数据及同比变化，可作为格式参考。

【供应链可持续】建议在供应商评估体系中纳入ESG评分维度，设定最低ESG合格线。近期优先行动：①开展供应商ESG自评问卷（2024年覆盖核心供应商≥80%）；②建立高风险供应商名单和整改追踪机制。

格式要求：每个议题用【议题名称】开头，包含"建议措施 + 近期优先行动（编号列举）+ 同行参考"三部分，段间空行分隔，不使用Markdown符号。

## 输出格式（JSON）
{
  "recommendations": [
    {
      "topic_name": "温室气体排放",
      "action": "建立Scope 1/2/3排放核算体系，参照GHG Protocol方法论",
      "priority": "high/medium/low",
      "reference_case": "同行企业XXX的做法：...",
      "standard_basis": "HKEX A1.2, ISSB S2-29"
    }
  ],
  "priority_actions": ["最重要行动1", "最重要行动2", "最重要行动3"],
  "generated_content": "整合所有建议后的完整披露优化建议文本（可直接参考使用）"
}

注意：
1. priority定义：high（必须立即执行）、medium（建议近期执行）、low（可考虑执行）
2. 每个major差距必须对应至少一条high priority建议
3. reference_case引用同行对比中的具体优秀做法
4. priority_actions只列出Top 3最重要的行动，按优先级排序
5. generated_content是一段完整的、可直接参考使用的披露优化建议文本，整合所有recommendations的内容

{% endif %}

