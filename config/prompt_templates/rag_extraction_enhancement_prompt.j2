{# RAG增强信息提取Prompt模板 #}
你是ESG信息提取专家。请基于原文和知识库参考内容，优化信息提取结果。

## 提取任务
- **字段名称**: {{ field_name }}
- **企业名称**: {{ company_name }}
- **报告年份**: {{ report_year }}

## 原始提取结果
```
{{ original_extraction }}
```

## 原文片段
```
{{ source_text }}
```

## 知识库参考内容
{{ retrieved_context }}

## 优化要求

请对原始提取结果进行以下检查和优化：

1. **完整性检查**
   - 原始提取是否完整？是否有遗漏的关键信息？
   - 数值是否包含单位？

2. **准确性验证**
   - 提取内容与原文是否一致？
   - 数值是否准确？

3. **知识库交叉验证**
   - 参考知识库中的类似披露，提取结果是否合理？
   - 是否存在明显异常（如数量级错误）？

4. **格式规范化**
   - 数值格式是否统一？
   - 单位是否标准？

## 输出格式（JSON）

```json
{
  "enhanced_extraction": "优化后的提取内容（如无优化则返回原始内容）",
  "optimization_reason": "优化原因说明",
  "confidence_level": "high/medium/low",
  "suggestions": [
    "建议1",
    "建议2"
  ],
  "needs_manual_review": true/false,
  "review_reason": "如需要人工复核，说明原因"
}
```

## 重要规则
- 如果原始提取已准确完整，直接返回原始内容
- 不要添加原文中没有的信息
- 不要修改数值，除非明显错误
- 优先保证准确性，宁可保守不要过度推断
